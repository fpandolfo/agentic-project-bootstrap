# Project Structure Steering

Canonical navigation owner: `docs/PROJECT_INDEX.md`.

Operational entrypoint: `AGENTS.md`.

Principles:

- one canonical owner per durable decision;
- thin entrypoints that route rather than duplicate;
- deep modules with narrow interfaces;
- no folder/file creation merely for visual symmetry;
- tests and deterministic tooling should make important behavior discoverable.
