# Architecture Decision Records

Use ADRs for decisions whose **rationale** will matter later.

Create one when multiple credible options exist, reversal would be expensive, the decision changes system boundaries, security/data ownership is affected, or future agents are likely to ask "why was this chosen?".

Do not create an ADR for every implementation detail.

Start from `docs/templates/ADR.md`.
